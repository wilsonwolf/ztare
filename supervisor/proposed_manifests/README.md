# Proposed Manifests

These are pre-registry planning artifacts.

Use them when a seed needs a bounded packet plan before a real program is opened.

Rules:
- proposals do not make a program active
- proposals do not belong in `program_registry.json`
- proposals become routable only after human approval, genesis, and registry entry
