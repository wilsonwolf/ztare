# Notes

Why this candidate exists:

- It is more original than the standard "monetary union without fiscal union" frame.
- It may produce cleaner forward observables than the current broad durability thesis.

What still needs grounding:

- how to define response latency without an arbitrary threshold
- which historical episodes count as valid latency tests
- whether the key benchmark is implementation delay, announcement delay, or credibility-formation delay

Expected difficulty:

- medium to high
- more novel, but also more exposed to threshold-design problems
