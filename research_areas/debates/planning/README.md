# Planning Debates

These debates are for pre-registry planning work.

They sit between:
- a seed spec in `research_areas/seeds/`
- and an accepted routable program in `supervisor/program_registry.json`

They should only discuss:
- packet decomposition
- pipeline type (`build`, `research`, `product`)
- bounded deliverables
- why a proposal should or should not be promoted into a real program
