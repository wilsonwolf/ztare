# Program Plans

This directory holds rendered human-readable plans for active, registry-level programs.

Source of truth:
- `supervisor/program_manifests/`

These files are post-registry artifacts. They should not be reused for pre-registry proposal planning.
