# Config

Operational non-code assets live here.

- `prompts/` — extraction, merge, history, brief, refinement, and QA prompts
- `renderers/` — audience-facing synthesis templates

Rubrics remain at repo root in `rubrics/` because they are first-class validator inputs.
